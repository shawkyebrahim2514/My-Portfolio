; Several Sanity Studio plugins still declare a react@^18 peer dependency even
; though they work with the react@19 that Sanity 6 itself requires. Accept the
; (harmless) peer mismatch so installs are reproducible.
legacy-peer-deps=true
